type: improvement
improvement:
  description: |-
    Add generated CLI support for:
    - binary args via file, base64-encoded strings, and stdin
    - collection and complex types support input from file and stdin (in addition to existing json-encoded strings)
  links:
  - https://github.com/palantir/conjure-go/pull/306
