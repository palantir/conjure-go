yml: |
  types:
    definitions:
      objects:
  services:
    FooService:
      name: "Foo Service"
      package: com.palantir.foo
      base-path: /foo
      endpoints:
        bar:
          http: GET /bar
          args:
            listOfStrings:
              param-type: query
              type: list<string>
