services:
  TestService:
    name: Test Service
    package: api
    endpoints:
      echo:
        docs: |
          Some echo docs here
          with newlines
        deprecated: |
          Use something
          else
          with
          newlines
        http: POST /echo
        args:
          input:
            type: string
        returns: string
